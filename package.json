{
  "name": "import-export-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"green,blue\" \"npm start --prefix backend\" \"npm run dev --prefix import-export-site\"",
    "install-all": "npm install --prefix backend && npm install --prefix import-export-site"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.1"
  }
}
